# OJ 3167: [Recommend] FizzBuzz

> - **iJudge cp_id**: 3167 — ภาษา Python
> - **เวลาจำกัด**: 1 วินาที | **หน่วยความจำ**: 32,000 KB
> - **หมดเขต**: 04 ก.ย. 2026, 00:00

---

## 1. โจทย์จริงจาก iJudge

จงเขียนผังงานสำหรับรับเลขจำนวนเต็มบวกเข้ามา 1 จำนวน

จากนั้นให้แสดงเลข 1 ถึง จำนวนเต็มบวกนั้น โดยจะมีเงื่อนไขดังนี้

- โดยถ้าเลขหาร 3 ลงตัวให้แสดงคำว่า `Fizz` แทน
- แต่ถ้าเลขหาร 5 ลงตัว ให้แสดงคำว่า `Buzz`
- แต่ถ้าเลขหารทั้ง 3 และ 5 ลงตัวให้แสดงคำว่า `FizzBuzz`

## 2. Input Specification

**1 บรรทัด**

- จำนวนเต็มบวก

## 3. Output Specification

**n บรรทัด**

- ตัวเลข, `Fizz`, `Buzz` หรือ `FizzBuzz`

## 4. ตัวอย่างจาก iJudge

### ตัวอย่างที่ 1
- **อินพุต**:
  ```text
  15
  ```
- **เอาต์พุต**:
  ```text
  1
  2
  Fizz
  4
  Buzz
  Fizz
  7
  8
  Fizz
  Buzz
  11
  Fizz
  13
  14
  FizzBuzz
  ```

## 5. จุดสำคัญและเทคนิคที่ใช้

### 5.1 ตัวดำเนินการหารเอาเศษ (`%`)
เครื่องหมาย `%` ใช้หาเศษจากการหาร ดังนั้น `i % 3 == 0` หมายความว่า "`i` หารด้วย 3 ลงตัว"

```python
15 % 3   # ได้ 0  -> หารลงตัว
14 % 3   # ได้ 2  -> หารไม่ลงตัว
```

### 5.2 จุดตายของข้อนี้: ลำดับการเช็กเงื่อนไข
เลขที่หารด้วย 15 ลงตัว (เช่น 15, 30, 45) ก็หารด้วย 3 ลงตัว **และ** หารด้วย 5 ลงตัวด้วย
ดังนั้นต้องเช็กเงื่อนไขที่ **เจาะจงที่สุดก่อนเสมอ**:

```python
if i % 15 == 0:        # ✅ เช็ก FizzBuzz ก่อน
    print("FizzBuzz")
elif i % 3 == 0:
    print("Fizz")
elif i % 5 == 0:
    print("Buzz")
else:
    print(i)
```

**ถ้าเรียงผิดจะเกิดอะไรขึ้น:**

```python
if i % 3 == 0:         # ❌ ผิด!
    print("Fizz")
elif i % 15 == 0:      # บรรทัดนี้ไม่มีทางทำงานเลย
    print("FizzBuzz")
```
เลข `15` จะเข้าเงื่อนไขแรก (`15 % 3 == 0` เป็นจริง) แล้วพิมพ์ `Fizz` ออกมา — `elif` ข้างล่างไม่มีโอกาสได้ทำงาน
ตัวอย่างทางการมี `n = 15` พอดี จึงจับผิดข้อนี้ได้ทันที

### 5.3 `% 15` หรือ `% 3 and % 5` ก็ได้
```python
if i % 15 == 0:                        # แบบสั้น
if i % 3 == 0 and i % 5 == 0:          # แบบชัดเจนว่ามาจากเงื่อนไขไหน
```
ทั้งสองถูกต้องเท่ากัน เพราะ 15 คือ ค.ร.น. ของ 3 กับ 5 — เลือกแบบที่ตัวเองอธิบายได้

### 5.4 ทำไมต้อง `range(1, n + 1)`
`range(a, b)` ใน Python จะ **ไม่รวม** `b`
ถ้าเขียน `range(1, n)` จะได้แค่ 1 ถึง `n - 1` ขาดตัวสุดท้ายไป
ต้องเขียน `range(1, n + 1)` จึงจะได้ครบ 1 ถึง `n`

### 5.5 `print(i)` ไม่ต้องแปลง `str()`
ฟังก์ชัน `print()` จะแปลงชนิดข้อมูลเป็นข้อความให้อัตโนมัติอยู่แล้ว จึงไม่จำเป็นต้องเขียน `print(str(i))`

### 5.6 เคล็ดลับ PEP-8 & Pylint: `not i % 15`
ใน Python ค่า `0` มีค่าความจริงเป็นเท็จ (Falsey) ในการตรวจสอบเงื่อนไข Pylint จึงแนะนำให้ย่อรูปจาก `i % 15 == 0` เป็น `not i % 15`:
```python
if not i % 15:
    print("FizzBuzz")
elif not i % 3:
    print("Fizz")
elif not i % 5:
    print("Buzz")
else:
    print(i)
```
ทั้งสองแบบให้ผลลัพธ์เหมือนกัน 100% แต่ `not i % 15` ช่วยลด warning ของ Pylint และได้คะแนน PEP-8 เต็ม 9,900 คะแนน

## 6. เทสเคสที่ควรลองเพิ่ม

| อินพุต | เอาต์พุตที่ควรได้ | ทดสอบอะไร |
| :--- | :--- | :--- |
| `15` | ตามตัวอย่างทางการ | **ตัวจับผิดลำดับเงื่อนไข** (บรรทัดที่ 15 ต้องเป็น `FizzBuzz`) |
| `5` | `1 / 2 / Fizz / 4 / Buzz` | ช่วงสั้น เช็ก Fizz กับ Buzz เดี่ยว ๆ |
| `1` | `1` | กรณีขอบเขตน้อยสุด |
| `3` | `1 / 2 / Fizz` | Fizz ตัวแรก |
| `30` | บรรทัดที่ 15 และ 30 = `FizzBuzz` | เช็กตัวคูณร่วมมากกว่า 1 ครั้ง |

## 7. เช็กลิสต์ก่อนส่ง
- [ ] ขอบเขตลูปเป็น `range(1, n + 1)`
- [ ] เช็กตัวคูณร่วม (`% 15 == 0`) **ก่อน** เช็กตัวคูณเดี่ยว
- [ ] สะกดตัวพิมพ์ใหญ่-เล็กถูกต้อง: `Fizz`, `Buzz`, `FizzBuzz`
- [ ] ทดสอบด้วย `n = 15` แล้วเช็กบรรทัดสุดท้ายว่าเป็น `FizzBuzz`
- [ ] ตรวจ PEP-8 ผ่านแล้ว
